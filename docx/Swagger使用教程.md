# Swagger UI 使用指南（图文教程）

## 🎯 第一步：访问文档

在浏览器中打开：**http://localhost:3000/api-docs**

你会看到这样的界面：

```
┌────────────────────────────────────────────────────────────────┐
│  My Node Server API                              📖 v1.0.0    │
│  基于 Express + TypeScript + MongoDB 的后端 API 文档          │
│  ─────────────────────────────────────────────────────────────│
│  Servers: ▼ http://localhost:3000 - 开发环境                 │
│                                                                │
│  🔓 Authorize                                                  │
│  ─────────────────────────────────────────────────────────────│
│                                                                │
│  🔐 Authentication  用户认证相关接口                           │
│    ▼ POST /api/backend/auth/LoginAuther   用户登录            │
│    ▼ POST /api/backend/auth/Register      用户注册            │
│                                                                │
│  👥 Users  用户管理相关接口                                    │
│    ▼ GET    /api/backend/user/              获取所有用户列表  │
│    ▼ GET    /api/backend/user/getUserId     根据ID获取用户    │
│    ▼ POST   /api/backend/user/CreateUser    创建新用户        │
│    ▼ PUT    /api/backend/user/updataUser    更新用户信息      │
│    ▼ DELETE /api/backend/user/deleteUser    删除用户          │
│                                                                │
│  💚 Health  健康检查                                           │
│    ▼ GET /api/                              API基本信息       │
└────────────────────────────────────────────────────────────────┘
```

---

## 🎯 第二步：测试登录接口

### 1. 点击 `POST /api/backend/auth/LoginAuther` 展开接口

```
▼ POST /api/backend/auth/LoginAuther   用户登录
  ┌────────────────────────────────────────────────┐
  │ 用户登录                                        │
  │ 使用邮箱和密码登录系统，成功后返回 JWT Token    │
  │                                                │
  │ [ Try it out ]                                 │
  │                                                │
  │ Request body  (required)                       │
  │ ┌────────────────────────────────────────────┐ │
  │ │ {                                          │ │
  │ │   "userName": "john",                      │ │
  │ │   "email": "john@example.com",             │ │
  │ │   "password": "123456"                     │ │
  │ │ }                                          │ │
  │ └────────────────────────────────────────────┘ │
  │                                                │
  │ [ Execute ]                                    │
  │                                                │
  │ Responses                                      │
  │ 200 - 登录成功                                 │
  │ 401 - 邮箱或密码错误                           │
  │ 500 - 服务器内部错误                           │
  └────────────────────────────────────────────────┘
```

### 2. 点击 **"Try it out"** 按钮

界面会变成可编辑状态

### 3. 修改请求参数（或使用默认值）

```json
{
  "userName": "testuser",
  "email": "test@example.com",
  "password": "123456"
}
```

### 4. 点击 **"Execute"** 按钮

### 5. 查看响应结果

```
Server response
─────────────────────────────────────────────
Code: 200

Response body
{
  "message": "Login successful",
  "userName": "testuser",
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyTmFtZ...",
  "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2Vy..."
}

Response headers
content-type: application/json; charset=utf-8
```

### 6. 复制 token 值

把 `token` 字段的值复制下来（不包括引号）：
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyTmFtZ...
```

---

## 🔐 第三步：设置认证 Token

### 1. 点击页面右上角的 🔓 **"Authorize"** 按钮

```
┌────────────────────────────────────────────────┐
│  Available authorizations                      │
│                                                │
│  bearerAuth  (http, Bearer)                    │
│  ┌──────────────────────────────────────────┐ │
│  │ Value:                                   │ │
│  │ Bearer eyJhbGciOiJIUzI1NiIsInR5cCI...   │ │
│  │                                          │ │
│  │ 在请求头中添加                            │ │
│  │ Authorization: Bearer {token}            │ │
│  └──────────────────────────────────────────┘ │
│                                                │
│  [ Authorize ]  [ Close ]                      │
└────────────────────────────────────────────────┘
```

### 2. 在输入框中输入

格式：`Bearer <你的token>`

**注意**：`Bearer` 后面有一个**空格**！

```
Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyTmFtZ...
```

### 3. 点击 **"Authorize"** 按钮

### 4. 点击 **"Close"** 关闭弹窗

现在 🔓 图标会变成 🔒，表示已认证

---

## 🎯 第四步：测试需要认证的接口

### 1. 点击 `GET /api/backend/user/` 展开接口

```
▼ GET /api/backend/user/   获取所有用户列表  🔒
  ┌────────────────────────────────────────────────┐
  │ 获取所有用户列表                                │
  │ 获取系统中所有用户的信息（按创建时间倒序）      │
  │                                                │
  │ [ Try it out ]                                 │
  │                                                │
  │ No parameters                                  │
  │                                                │
  │ [ Execute ]                                    │
  └────────────────────────────────────────────────┘
```

### 2. 点击 **"Try it out"** → **"Execute"**

### 3. 查看响应结果

```
Server response
─────────────────────────────────────────────
Code: 200

Response body
{
  "success": true,
  "count": 3,
  "data": [
    {
      "userId": "123e4567-e89b-12d3-a456-426614174000",
      "name": "张三",
      "age": 25,
      "avatarUrl": "https://example.com/avatar.jpg",
      "bio": "热爱编程的开发者",
      "location": "北京",
      "preferences": {
        "theme": "light",
        "language": "zh-CN"
      }
    },
    // ... 更多用户
  ]
}
```

---

## 📝 第五步：测试创建用户接口

### 1. 点击 `POST /api/backend/user/CreateUser` 展开

### 2. 点击 **"Try it out"**

### 3. 编辑请求体

```json
{
  "userId": "123e4567-e89b-12d3-a456-426614174000",
  "name": "李四",
  "age": 28,
  "avatarUrl": "https://example.com/avatar2.jpg",
  "bio": "全栈工程师",
  "location": "上海",
  "preferences": {
    "theme": "dark",
    "language": "zh-CN"
  }
}
```

### 4. 点击 **"Execute"**

### 5. 查看响应

```
Code: 201

Response body
{
  "success": true,
  "data": {
    "userId": "123e4567-e89b-12d3-a456-426614174000",
    "name": "李四",
    "age": 28,
    // ... 其他字段
    "_id": "507f1f77bcf86cd799439011",
    "createdAt": "2024-01-07T12:00:00.000Z",
    "updatedAt": "2024-01-07T12:00:00.000Z"
  }
}
```

---

## 💡 常见问题

### ❌ 401 Unauthorized

**问题**：测试需要认证的接口时返回 401

**原因**：没有设置 Token 或 Token 过期

**解决**：
1. 重新登录获取新 Token
2. 点击 🔓 "Authorize" 设置 Token
3. 确保格式是 `Bearer <token>`（有空格）

### ❌ 400 Bad Request

**问题**：请求参数格式错误

**原因**：缺少必填字段或字段类型不对

**解决**：
1. 查看接口文档中的 "required" 标记
2. 检查字段类型（string、number、object）
3. 参考示例值填写

### ❌ 500 Internal Server Error

**问题**：服务器内部错误

**原因**：后端代码有 bug 或数据库连接问题

**解决**：
1. 查看终端的错误日志
2. 检查数据库是否连接成功
3. 检查必填字段是否满足验证规则

---

## 🎨 界面元素说明

```
┌─────────────────────────────────────────────┐
│  HTTP 方法颜色标识：                         │
│  ─────────────────────────────────────────  │
│  🟢 GET     - 绿色（获取资源）               │
│  🔵 POST    - 蓝色（创建资源）               │
│  🟠 PUT     - 橙色（更新资源）               │
│  🔴 DELETE  - 红色（删除资源）               │
│                                             │
│  其他标识：                                  │
│  ─────────────────────────────────────────  │
│  🔒 - 需要认证（Bearer Token）               │
│  ⚠️  - 已弃用的接口                          │
│  ⭐ - 推荐使用                               │
└─────────────────────────────────────────────┘
```

---

## 🔗 快捷键

- **Ctrl + F** / **Cmd + F**：在页面中搜索接口
- **展开/收起**：点击接口名称左侧的 ▶ / ▼
- **展开所有**：点击某个标签（如 Authentication）可展开该分组下所有接口

---

## 📤 导出到其他工具

### 方法一：复制 JSON URL

```
http://localhost:3000/api-docs.json
```

### 方法二：下载 JSON 文件

在浏览器中访问上面的 URL，然后 Ctrl+S 保存

### 方法三：直接在 Postman 中导入

1. 打开 Postman
2. **Import** → **Link**
3. 粘贴：`http://localhost:3000/api-docs.json`
4. **Continue** → **Import**

---

## 🎯 实战练习

### 练习1：完整的用户注册和登录流程

1. 使用 `POST /api/backend/auth/Register` 注册新用户
2. 使用 `POST /api/backend/auth/LoginAuther` 登录
3. 复制 token 并设置到 Authorize
4. 使用 `GET /api/backend/user/` 查看用户列表

### 练习2：CRUD 操作

1. 使用 `POST /api/backend/user/CreateUser` 创建用户资料
2. 使用 `GET /api/backend/user/getUserId` 查询该用户
3. 使用 `PUT /api/backend/user/updataUser` 更新用户信息
4. 使用 `DELETE /api/backend/user/deleteUser` 删除用户

---

## ✨ 小技巧

1. **查看请求的 cURL 命令**：
   执行后可以看到等效的 cURL 命令，方便在终端测试

2. **查看 Schema**：
   点击 Response 示例旁边的 "Schema" 标签可以看到数据结构

3. **查看所有数据模型**：
   滚动到页面最底部可以看到 "Schemas" 部分，查看所有数据模型定义

4. **切换服务器**：
   如果配置了多个服务器（开发/生产），可以在顶部切换

---

现在你已经完全掌握如何使用 Swagger UI 了！🎉

开始测试你的 API 吧：**http://localhost:3000/api-docs**

